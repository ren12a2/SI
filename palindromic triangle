#include <stdio.h>
#include <stdlib.h>

int main() {

    int n;
    scanf("%d", &n);

    char c='A';
    for(int i=0; i<n; i++){
        char c='A';
        for(int j=0; j<=i; j++){
            printf("%c ", c++);
        }

        c-=2;

        for(int j=0; j<i; j++){
            printf("%c ", c--);
        }

        printf("\n");
    }

    return 0;
}

Example

Input

5



Output

A

A B A

A B C B A

A B C D C B A

A B C D E D C B A
